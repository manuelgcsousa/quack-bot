module.exports = {
	name: 'sonzaço',
	description: 'Fazer deploy daquele somzaço que vocês sabem mpts.',
	execute(message, args) {
		if (args.length > 1)
			return;

		if (args.length === 0)
			// Dar a lista de links.

		// Adicionar um link à lista.
		
		

		message.channel.send('cartucheira carregada oh filho!');
	}
};
